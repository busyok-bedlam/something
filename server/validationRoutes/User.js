import generatorMessages from './generatorMessages';
import di from '../di';
const config = di.get('config');
const {countriesConfig, commonConfig} = config;

export default class User {
    async signInUser(ctx, next) {

    }

    async signUpUser(ctx, next) {

    }
}
